import { db } from './firebase-config.js';import { collection, addDoc, serverTimestamp } from 'https://www.gstatic.com/firebasejs/10.12.5/firebase-firestore.js';import { qs, toast, cacheGet, cacheSet } from './utils.js';
qs('#feedbackForm')?.addEventListener('submit',async(e)=>{e.preventDefault();const text=qs('#publicFeedbackText').value.trim();if(!text)return;const payload={text,createdAt:new Date().toISOString()};try{await addDoc(collection(db,'feedback'),{...payload,createdAt:serverTimestamp()});await addDoc(collection(db,'notifications'),{type:'feedback',summary:text.slice(0,50),payload,isRead:false,createdAt:serverTimestamp()});}catch{const arr=cacheGet('kh_feedback',[]);arr.push(payload);cacheSet('kh_feedback',arr);}toast('Thanks for feedback');e.target.reset();});
